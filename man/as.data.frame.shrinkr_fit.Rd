% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{as.data.frame.shrinkr_fit}
\alias{as.data.frame.shrinkr_fit}
\title{Convert shrinkr_fit to data.frame}
\usage{
\method{as.data.frame}{shrinkr_fit}(
  x,
  row.names = NULL,
  optional = FALSE,
  variables = NULL,
  include_internals = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{row.names}{NULL or character vector giving row names.}

\item{optional}{Logical; if \code{TRUE}, setting row names and converting
column names is optional.}

\item{variables}{Character vector of parameter names to extract.
If \code{NULL}, returns all user-facing parameters (excludes internals).}

\item{include_internals}{Logical; if \code{TRUE}, includes internal Stan parameters.
Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{as_draws_df()}.}
}
\value{
A data.frame with columns for chain, iteration, draw, and
requested parameters.
}
\description{
Extracts posterior draws as a regular data frame. This is a convenience
wrapper around \code{as_draws_df()} that returns a plain data.frame.
}
\examples{
\dontrun{
fit <- shrink(mixture = mix, hierarchical_priors = priors)

# Extract as data frame
draws_df <- as.data.frame(fit)
head(draws_df)

# Just mu and tau
mu_tau_df <- as.data.frame(fit, variables = c("mu", "tau"))
}
}
\seealso{
\code{\link[=as_draws_df.shrinkr_fit]{as_draws_df.shrinkr_fit()}} for posterior package format,
\code{\link[=extract_mu_tau]{extract_mu_tau()}} for hyperparameters only
}
