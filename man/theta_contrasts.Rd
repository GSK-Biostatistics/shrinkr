% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{theta_contrasts}
\alias{theta_contrasts}
\title{Linear combinations of theta}
\usage{
theta_contrasts(fit, contrast_matrix, labels = NULL)
}
\arguments{
\item{fit}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{contrast_matrix}{A numeric matrix L with \code{ncol(L) = G} (number of groups)
and \code{nrow(L) = M} (number of contrasts). Each row defines one linear combination:
\deqn{contrast_i = L_{i1}\theta_1 + L_{i2}\theta_2 + \ldots + L_{iG}\theta_G}}

\item{labels}{Optional character vector of length M to name the contrasts.
If \code{NULL}, uses "contrast1", "contrast2", etc.}
}
\value{
A \code{posterior::draws_df} with columns \code{.chain}, \code{.iteration}, \code{.draw},
and one column per contrast.
}
\description{
Computes posterior draws for linear combinations of subgroup effects.
Useful for pairwise contrasts (e.g., treatment vs control), weighted
averages, or any custom linear estimand involving theta parameters.
}
\examples{
\dontrun{
fit <- shrink(mixture = mix, hierarchical_priors = priors)

# Pairwise contrast: group 2 vs group 1
L <- matrix(c(-1, 1, 0, 0), nrow = 1)
contrast <- theta_contrasts(fit, L, labels = "Trt2_vs_Trt1")

# Multiple contrasts
L <- rbind(
  c(-1, 1, 0, 0),   # Group 2 vs 1
  c(-1, 0, 1, 0),   # Group 3 vs 1
  c(0, -1, 1, 0)    # Group 3 vs 2
)
contrasts <- theta_contrasts(
  fit, L,
  labels = c("2vs1", "3vs1", "3vs2")
)

# Summarize contrasts
library(posterior)
summarise_draws(contrasts)

# Visualize
library(bayesplot)
mcmc_areas(contrasts, regex_pars = ".*")

# Probability that contrast > 0
mean(contrasts$`2vs1` > 0)
}
}
\seealso{
\code{\link[=shrink]{shrink()}} for fitting models,
\code{\link[=summarise_theta]{summarise_theta()}} for basic theta summaries
}
