% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{summarise_theta}
\alias{summarise_theta}
\alias{summarize_theta}
\title{Summarize theta parameters by group}
\usage{
summarise_theta(
  fit,
  probs = c(0.025, 0.5, 0.975),
  group_names = NULL,
  measures = NULL
)

summarize_theta(
  fit,
  probs = c(0.025, 0.5, 0.975),
  group_names = NULL,
  measures = NULL
)
}
\arguments{
\item{fit}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{probs}{Numeric vector of quantiles to compute. Default is
\code{c(0.025, 0.5, 0.975)} for 95\% credible intervals.}

\item{group_names}{Optional character vector of length G to label groups.
If \code{NULL}, uses names from \code{fit$data$vars} or defaults to "group1", etc.}

\item{measures}{Optional character vector or list of summary measures to compute.
If \code{NULL}, uses mean, sd, and convergence diagnostics.}
}
\value{
A data frame (tibble if available) with one row per group and columns:
\describe{
\item{\code{group}}{Group identifier}
\item{\code{mean}}{Posterior mean}
\item{\code{sd}}{Posterior standard deviation}
\item{\code{q2.5}, \code{q50}, \code{q97.5}}{Quantiles (or custom quantiles from \code{probs})}
\item{\code{rhat}}{R-hat convergence diagnostic}
\item{\code{ess_bulk}}{Effective sample size (bulk)}
\item{\code{ess_tail}}{Effective sample size (tail)}
}
}
\description{
Computes posterior summaries for subgroup effects (theta parameters).
Returns a data frame with one row per group containing posterior means,
standard deviations, quantiles, and convergence diagnostics.

This is a focused alternative to \code{summary(fit)}, which returns summaries
for all parameters including mu and tau.
}
\examples{
set.seed(1)
draws <- data.frame(
  mu = rnorm(20, 0.2, 0.05),
  tau = abs(rnorm(20, 0.3, 0.03)),
  `theta[1]` = rnorm(20, 0.0, 0.1),
  `theta[2]` = rnorm(20, 0.3, 0.1),
  `theta[3]` = rnorm(20, 0.5, 0.1),
  check.names = FALSE
)
draws$tau_squared <- draws$tau^2
fit <- list(
  fit = posterior::as_draws_df(draws),
  data = list(
    G = 3, K = 1, centered = FALSE,
    vars = c("group1", "group2", "group3"),
    quantiles = data.frame(
      q2.5 = c(-0.20, 0.10, 0.30),
      q50 = c(0.00, 0.30, 0.50),
      q97.5 = c(0.20, 0.50, 0.70)
    )
  ),
  summary = posterior::summarise_draws(
    posterior::as_draws_df(draws),
    "mean", "sd",
    ~posterior::quantile2(., probs = c(0.025, 0.5, 0.975))
  ),
  diagnostics = list(n_divergent = 0, max_treedepth = 0, n_leapfrog = 0)
)
class(fit) <- "shrinkr_fit"
summarise_theta(fit)
summarise_theta(fit, probs = c(0.05, 0.5, 0.95))
summarise_theta(fit, group_names = c("Control", "A", "B"))
}
\seealso{
\code{\link[=shrink]{shrink()}} for fitting models,
\code{\link[=summarise_mu_tau]{summarise_mu_tau()}} for hyperparameter summaries,
\code{\link[=theta_contrasts]{theta_contrasts()}} for computing contrasts
}
