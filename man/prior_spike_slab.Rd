% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_system.R
\name{prior_spike_slab}
\alias{prior_spike_slab}
\title{Spike-and-slab prior for testing homogeneity}
\usage{
prior_spike_slab(
  spike_location = 0,
  spike_scale = 0.01,
  slab_scale = 1,
  spike_prob = 0.5
)
}
\arguments{
\item{spike_location}{Location of the spike (default 0)}

\item{spike_scale}{Scale of the spike component (default 0.01)}

\item{slab_scale}{Scale of the slab component (default 1)}

\item{spike_prob}{Probability of the spike component (default 0.5)}
}
\value{
A spike-and-slab mixture distribution
}
\description{
Creates a mixture of two Normal distributions. Since tau is a scale parameter,
this must be wrapped in \code{\link[distributional:dist_truncated]{distributional::dist_truncated()}} with \code{lower = 0}
before passing to \code{\link[=shrink]{shrink()}}:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{tau_prior <- dist_truncated(prior_spike_slab(), lower = 0)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
library(distributional)
# Spike-and-slab for tau — must truncate for scale parameter
tau_prior <- dist_truncated(
  prior_spike_slab(spike_prob = 0.5, spike_scale = 0.01, slab_scale = 1),
  lower = 0
)
}
}
