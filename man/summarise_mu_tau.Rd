% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{summarise_mu_tau}
\alias{summarise_mu_tau}
\alias{summarize_mu_tau}
\title{Summarize mu and tau hyperparameters}
\usage{
summarise_mu_tau(fit, probs = c(0.025, 0.5, 0.975), measures = NULL)

summarize_mu_tau(fit, probs = c(0.025, 0.5, 0.975), measures = NULL)
}
\arguments{
\item{fit}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{probs}{Numeric vector of quantiles to compute. Default is
\code{c(0.025, 0.5, 0.975)} for 95\% credible intervals.}

\item{measures}{Optional character vector or list of summary measures to compute.
If \code{NULL}, uses mean, sd, and convergence diagnostics.}
}
\value{
A data frame (tibble if available) with one row per parameter and columns:
\describe{
\item{\code{parameter}}{Parameter name (\code{mu}, \code{tau}, or \code{tau_squared})}
\item{\code{mean}}{Posterior mean}
\item{\code{sd}}{Posterior standard deviation}
\item{\code{q2.5}, \code{q50}, \code{q97.5}}{Quantiles (or custom quantiles from \code{probs})}
\item{\code{rhat}}{R-hat convergence diagnostic}
\item{\code{ess_bulk}}{Effective sample size (bulk)}
\item{\code{ess_tail}}{Effective sample size (tail)}
}
}
\description{
Computes posterior summaries for the hierarchical hyperparameters
(\code{mu}, \code{tau}, and \code{tau_squared}). Returns a data frame with one row per
parameter containing posterior means, standard deviations, quantiles, and
convergence diagnostics.

This is a focused alternative to \code{summary(fit)}, which returns summaries
for all parameters including theta.
}
\examples{
\dontrun{
fit <- shrink(mixture = mix, hierarchical_priors = priors)

# Basic summary
mu_tau_summary <- summarise_mu_tau(fit)
print(mu_tau_summary)

# Custom quantiles
mu_tau_summary <- summarise_mu_tau(fit, probs = c(0.05, 0.5, 0.95))

# Custom measures
mu_tau_summary <- summarise_mu_tau(fit, measures = c("mean", "median", "mad"))
}
}
\seealso{
\code{\link[=shrink]{shrink()}} for fitting models,
\code{\link[=extract_mu_tau]{extract_mu_tau()}} for raw hyperparameter draws,
\code{\link[=summarise_theta]{summarise_theta()}} for group-level summaries
}
