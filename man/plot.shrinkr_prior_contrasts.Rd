% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_predictive.R
\name{plot.shrinkr_prior_contrasts}
\alias{plot.shrinkr_prior_contrasts}
\title{Plot prior predictive pairwise differences}
\usage{
\method{plot}{shrinkr_prior_contrasts}(x, by_pair = FALSE, ...)
}
\arguments{
\item{x}{A \code{shrinkr_prior_contrasts} object from
\code{\link{prior_pairwise_differences}}.}

\item{by_pair}{Logical; if \code{TRUE}, facet by pair. If \code{FALSE} (default),
pool all pairwise differences into a single plot.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot2 object.
}
\description{
Creates a density plot of \eqn{|\theta_i - \theta_j|}
from prior predictive samples. Useful for calibrating hierarchical priors.
Styling matches \code{\link[=plot.shrinkr_prior_pred]{plot.shrinkr_prior_pred()}} for visual consistency.
}
\examples{
\dontrun{
library(distributional)

priors <- list(
  mu = dist_normal(0, 5),
  tau = dist_truncated(dist_normal(0, 1), lower = 0)
)

prior_pred <- sample_prior_predictive(priors, n_groups = 4, n_draws = 2000)
pw <- prior_pairwise_differences(prior_pred)

# Pooled density
plot(pw)

# Faceted by pair
plot(pw, by_pair = TRUE)
}
}
\seealso{
\code{\link{prior_pairwise_differences}}
}
