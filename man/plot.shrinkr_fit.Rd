% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{plot.shrinkr_fit}
\alias{plot.shrinkr_fit}
\title{Plot shrinkage fit}
\usage{
\method{plot}{shrinkr_fit}(
  x,
  type = c("shrinkage", "diagnostics"),
  group_names = NULL,
  show_arrows = FALSE,
  show_intervals = TRUE,
  interval_prob = 0.95,
  point_size = 3,
  arrow_alpha = 0.6,
  dodge_width = 0.3,
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{type}{Character; type of plot. Options:
\itemize{
\item \code{"shrinkage"} - Basic shrinkage visualization (default)
\item \code{"diagnostics"} - Multi-panel with hyperparameters and shrinkage factor
}}

\item{group_names}{Optional character vector of length G to label groups.
If \code{NULL}, uses names from \code{x$data$vars} or defaults to "group1", etc.}

\item{show_arrows}{Logical; draw arrows from pre-shrunk to post-shrunk estimates?
Default \code{FALSE}. Only applies when \code{type = "shrinkage"}.}

\item{show_intervals}{Logical; show credible intervals for both pre-shrunk and
post-shrunk estimates? Default \code{TRUE}. Only applies when \code{type = "shrinkage"}.}

\item{interval_prob}{Numeric; probability mass for credible intervals.
Default 0.95 for 95\% intervals. Only applies when \code{type = "shrinkage"}.}

\item{point_size}{Numeric; size of points. Default 3.}

\item{arrow_alpha}{Numeric; transparency of arrows (0-1). Default 0.6.
Only applies when \code{show_arrows = TRUE}.}

\item{dodge_width}{Numeric; horizontal spacing between pre-shrunk and
post-shrunk estimates in the side-by-side display. Default 0.3.
Larger values increase separation between estimate types.}

\item{title}{Character; plot title. If \code{NULL}, uses default title.}

\item{subtitle}{Character; plot subtitle. If \code{NULL}, auto-generates from
global mean and tau.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot2 object (for \code{type = "shrinkage"}), or a patchwork object/list
(for \code{type = "diagnostics"}).
}
\description{
Visualizes the hierarchical shrinkage model fit. Creates either:
\itemize{
\item \code{"shrinkage"} — Shows pre/post-shrunk estimates with arrows
\item \code{"diagnostics"} — Multi-panel view with hyperparameters and shrinkage factor
}

The shrinkage plot displays:
\itemize{
\item \strong{Pre-shrunk estimates} (hollow circles) — from stage 1 mixture or MLEs
\item \strong{Post-shrunk estimates} (filled circles) — posterior means of theta
\item \strong{Global mean} (dashed line) — posterior mean of mu
\item \strong{Credible intervals} (optional) — for shrunken estimates
\item \strong{Arrows} (optional) — showing direction and magnitude of shrinkage
}
}
\examples{
# Plotting requires a fitted shrinkr_fit object from shrink().
# The full example is not run because it fits a Stan model.
\dontrun{
fit <- shrink(mixture = mix, hierarchical_priors = priors)
plot(fit)
plot(fit, show_arrows = TRUE, interval_prob = 0.95)
}
}
\seealso{
\code{\link[=shrink]{shrink()}} for fitting models,
\code{\link[=extract_mu_tau]{extract_mu_tau()}} for hyperparameter draws
}
