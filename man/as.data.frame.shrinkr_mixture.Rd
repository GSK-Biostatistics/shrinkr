% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixture-methods.R
\name{as.data.frame.shrinkr_mixture}
\alias{as.data.frame.shrinkr_mixture}
\title{Convert mixture fit to data frame}
\usage{
\method{as.data.frame}{shrinkr_mixture}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{shrinkr_mixture} object from \code{\link{fit_mixture}}.}

\item{row.names}{Ignored (for S3 consistency).}

\item{optional}{Ignored (for S3 consistency).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A data frame (or tibble if available) containing the component
specifications with columns:
\item{component}{Component number (1 to K)}
\item{variable}{Variable name}
\item{weight}{Component weight (mixing proportion)}
\item{mean}{Component mean for this variable}
\item{sd}{Component marginal standard deviation for this variable}
}
\description{
Converts a fitted mixture model to a tidy long-format data frame.
This is essentially an accessor for the \code{components} element.
}
\examples{
\dontrun{
samples <- list(
  matrix(rnorm(2000, 0.0, 0.5), ncol = 1),
  matrix(rnorm(2000, 0.5, 0.5), ncol = 1),
  matrix(rnorm(2000, 1.0, 0.5), ncol = 1)
)

mix <- fit_mixture(samples, K_max = 5)

# Convert to data frame
df <- as.data.frame(mix)
head(df)

# Use with dplyr
library(dplyr)
df \%>\%
  group_by(variable) \%>\%
  summarise(weighted_mean = sum(weight * mean))
}
}
\seealso{
\code{\link{fit_mixture}} for fitting mixture models
}
