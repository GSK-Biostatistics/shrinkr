% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{as_draws_df.shrinkr_fit}
\alias{as_draws_df.shrinkr_fit}
\title{Convert shrinkr_fit to draws_df}
\usage{
\method{as_draws_df}{shrinkr_fit}(x, variables = NULL, include_internals = FALSE, ...)
}
\arguments{
\item{x}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{variables}{Character vector of parameter names to extract. Options include:
\itemize{
\item \code{"mu"} - Global mean
\item \code{"tau"} - Heterogeneity SD
\item \code{"tau_squared"} - Heterogeneity variance
\item \code{"theta"} or \code{"theta[i]"} - Subgroup effects
}
If \code{NULL} (default), returns all user-facing parameters.}

\item{include_internals}{Logical; if \code{TRUE}, includes internal Stan parameters
(\code{theta_c}, \code{z}) used for parameterization. Default \code{FALSE}. Only applies
when \code{variables = NULL}.}

\item{...}{Additional arguments passed to \code{posterior::as_draws_df()}.}
}
\value{
A \code{posterior::draws_df} with columns for chain, iteration, draw,
and requested parameters.
}
\description{
Extracts posterior draws in tidy format using the \code{posterior} package.
By default returns user-facing parameters (mu, tau, theta, etc.) and
excludes internal parameterization details. Set \code{include_internals = TRUE}
to access all parameters including theta_c and z.
}
\examples{
\dontrun{
fit <- shrink(mixture = mix, hierarchical_priors = priors)

# User-facing parameters only (default)
all_draws <- as_draws_df(fit)
variables(all_draws)  # mu, tau, theta[1], ..., tau_squared

# Include internal parameters for diagnostics
all_draws_internal <- as_draws_df(fit, include_internals = TRUE)
variables(all_draws_internal)  # includes theta_c, z

# Just theta parameters
theta_draws <- as_draws_df(fit, variables = "theta")

# Specific thetas
theta12_draws <- as_draws_df(fit, variables = c("theta[1]", "theta[2]"))

# Work with draws
library(posterior)
summarise_draws(all_draws)
}
}
\seealso{
\code{\link[=shrink]{shrink()}} for fitting models,
\code{\link[=extract_mu_tau]{extract_mu_tau()}} for hyperparameters only
}
