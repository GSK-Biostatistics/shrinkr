% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixture-methods.R
\name{plot.shrinkr_mixture}
\alias{plot.shrinkr_mixture}
\title{Plot fitted marginal densities or QQ plots for mixture models}
\usage{
\method{plot}{shrinkr_mixture}(
  x,
  draws = NULL,
  variables = NULL,
  type = c("density", "qq"),
  overlay = c("hist", "kde", "both", "none"),
  bins = 50,
  kde_bw = NULL,
  show_components = TRUE,
  facet = TRUE,
  n_points = 501,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{shrinkr_mixture} object from \code{\link[=fit_mixture]{fit_mixture()}}.}

\item{draws}{Optional samples to show as histogram/KDE or for QQ plot. Accepts any
input shape supported by \code{\link[=fit_mixture]{fit_mixture()}}. When \code{NULL}, only fitted curves are drawn
(QQ plot requires draws).}

\item{variables}{Character vector of variables to plot. Defaults to all
variables in \code{x$components$variable}. Variable names must match the names
created by the fitter (and by \code{.coerce_draws_df()}).}

\item{type}{One of \code{c("density","qq")}. Default \code{"density"} shows density overlay;
\code{"qq"} creates quantile-quantile plots comparing empirical vs fitted quantiles.}

\item{overlay}{One of \code{c("hist","kde","both","none")}. Default \code{"hist"}.
Only applies when \code{type = "density"}.}

\item{bins}{Integer number of bins for the histogram (default \code{50}).}

\item{kde_bw}{Bandwidth for \code{stats::density()}; \code{NULL} uses the default.
Ignored unless \code{overlay} is \code{"kde"} or \code{"both"}.}

\item{show_components}{Logical; if \code{TRUE} (default) overlays per-component
curves using component weights, means, and \strong{marginal} SDs from
\code{x$components}. Only applies when \code{type = "density"}.}

\item{facet}{Logical; if \code{TRUE} (default) facet by variable when plotting
more than one variable.}

\item{n_points}{Integer; number of x grid points for evaluating densities
(default \code{501}). For QQ plots, this controls the number of quantiles to compare.}

\item{verbose}{Logical; print brief matching diagnostics.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot2} object.
}
\description{
Overlays fitted \strong{marginal} mixture densities from a \code{shrinkr_mixture}
(returned by \code{\link[=fit_mixture]{fit_mixture()}}) on top of the \strong{observed samples} for
selected variables, OR creates QQ plots comparing empirical vs fitted quantiles.
The function uses the \emph{same coercion logic} as \code{\link[=fit_mixture]{fit_mixture()}} (via an internal
\code{.coerce_draws_df()} helper), ensuring that variable names line up even when
users pass a list of matrices.

\strong{Important:} For multivariate joint fits, this produces \strong{marginal}
overlays (one panel per variable when faceting). Each marginal density is
computed by summing the weighted component densities for that variable.
}
\details{
\subsection{Density plots}{

The total marginal density for each variable \eqn{j} is computed as
\deqn{f_j(x)=\sum_{k=1}^{K} w_k \,\phi\!\left(x \mid \mu_{jk},\, \sigma_{jk}\right),}
using per-component marginal SDs (\code{sd}) already stored in \code{x$components}.

The plotting range per variable is taken from the sample range if available
(with 5\% padding), otherwise from \verb{mean +/- 4*sd} across that variable's
components--avoiding non-finite \code{seq()} errors when samples are absent.
}

\subsection{QQ plots}{

When \code{type = "qq"}, the function creates quantile-quantile plots by:
\enumerate{
\item Computing empirical quantiles from the observed data
\item Computing theoretical quantiles from the fitted mixture CDF via numerical inversion
\item Plotting empirical vs theoretical quantiles with a 45-degree reference line
}

Points falling on the reference line indicate good agreement between the fitted
mixture and the data. Systematic deviations suggest model misfit.
}
}
\examples{
\dontrun{
# Suppose the user supplied a list of 1-col matrices:
samples <- list(
  matrix(rnorm(2000, 0.0, 0.5), ncol = 1),
  matrix(rnorm(2000, 0.5, 0.5), ncol = 1),
  matrix(rnorm(2000, 1.0, 0.5), ncol = 1)
)

mix <- fit_mixture(samples, K_max = 5)

# Density plot with histogram overlay
plot(mix, draws = samples, type = "density", variables = c("group1","group2"))

# QQ plot to check goodness of fit
plot(mix, draws = samples, type = "qq", variables = c("group1","group2"))

# Joint fit, plot all marginals with histogram + KDE:
plot(mix, draws = samples, type = "density", overlay = "both", facet = TRUE)
}

}
\seealso{
\code{\link[=fit_mixture]{fit_mixture()}} for fitting mixture models
}
