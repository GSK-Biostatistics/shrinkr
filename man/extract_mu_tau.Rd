% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink-methods.R
\name{extract_mu_tau}
\alias{extract_mu_tau}
\title{Extract mu and tau parameters}
\usage{
extract_mu_tau(x, ...)
}
\arguments{
\item{x}{A \code{shrinkr_fit} object from \code{\link[=shrink]{shrink()}}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{posterior::draws_df} with columns:
\describe{
\item{\code{.chain}}{Chain index}
\item{\code{.iteration}}{Iteration within chain}
\item{\code{.draw}}{Overall draw index}
\item{\code{mu}}{Global mean parameter}
\item{\code{tau}}{Heterogeneity parameter}
\item{\code{tau_squared}}{Variance (tau^2)}
}
}
\description{
Extracts posterior draws for the hyperparameters mu (global mean) and
tau (heterogeneity standard deviation) from a fitted shrinkage model.
}
\examples{
set.seed(1)
draws <- data.frame(
  mu = rnorm(20, 0.2, 0.05),
  tau = abs(rnorm(20, 0.3, 0.03)),
  `theta[1]` = rnorm(20, 0.0, 0.1),
  `theta[2]` = rnorm(20, 0.3, 0.1),
  `theta[3]` = rnorm(20, 0.5, 0.1),
  check.names = FALSE
)
draws$tau_squared <- draws$tau^2
fit <- list(
  fit = posterior::as_draws_df(draws),
  data = list(
    G = 3, K = 1, centered = FALSE,
    vars = c("group1", "group2", "group3"),
    quantiles = data.frame(
      q2.5 = c(-0.20, 0.10, 0.30),
      q50 = c(0.00, 0.30, 0.50),
      q97.5 = c(0.20, 0.50, 0.70)
    )
  ),
  summary = posterior::summarise_draws(
    posterior::as_draws_df(draws),
    "mean", "sd",
    ~posterior::quantile2(., probs = c(0.025, 0.5, 0.975))
  ),
  diagnostics = list(n_divergent = 0, max_treedepth = 0, n_leapfrog = 0)
)
class(fit) <- "shrinkr_fit"
mu_tau <- extract_mu_tau(fit)
summarise_mu_tau(fit)
posterior::summarise_draws(mu_tau)
}
\seealso{
\code{\link[=shrink]{shrink()}} for fitting models,
\code{\link[=summarise_mu_tau]{summarise_mu_tau()}} for summary statistics,
\code{\link[=as_draws_df.shrinkr_fit]{as_draws_df.shrinkr_fit()}} for all parameters
}
